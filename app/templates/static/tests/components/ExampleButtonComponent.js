/*
module('ExampleButtonComponent');

test('ExampleButtonComponent is an Ember Component', function () {
	ok(Ember.Component.detect(App.ExampleButtonComponent), "App.ExampleButtonComponent is an Ember.Component subclass");
});

test('isEnabled has an initial value of false', function () {
	var component = App.ExampleButtonComponent.create();
	equal(component.get('isEnabled'), false, "The component's isEnabled starts false");
});
*/
